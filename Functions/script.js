/* function alertName (myName) {
    alert (myName)
}

alertName ('Milana') */

// function generateRandomNumber() {
//     return Math.floor(Math.random() * 5) + 1;
//   }
//   generateRandomNumber(); 

  /* function getNameAndSurnameLength(name, surname) {
    return name.length + surname.length;
  }
  getNameAndSurnameLength('Milana', 'Kamziuk'); */

  /*
  function calculate(n1, n2, operator) {
  switch(operator){
    case 'sum':
      return n1 + n2;
    case 'sub':
      return n1 - n2;
    case 'div':
      return n1 / n2;
    case 'multi':
      return n1 * n2;
  }
}
calculate(1, 2, 'sum');
*/

/*
function generateRandomNumber() {
  return Math.floor(Math.random() * 10 + 1);
}

function squareNumber(number) {
  return Math.pow(number, 2);
}

squareNum(generateRandomNumber());
*/



/*function showDistance (speed, time) {
  console.log (speed * time)
}

showDistance (10,5)
*/


/*function sum (a, b) {
  let getSum = a + b;
  return getSum;
}

console.log (sum (5,4))*/

/*let myDistance = getDistance (5, 10)
function getDistance (speed, time) {
  let distance = speed * time;
  return distance
}

console.log (myDistance)*/

/*let arr=[-3, 1, -2, 4, -5, 6];
function keepPositive(data) {
 
 
  const positiveNumbers = [];
  for (let i = 0; i < data.length; i++) {
    if (data[i] > 0){
       
      positiveNumbers.push(data[i])
     }
 
  }
 
  return positiveNumbers;
}
console.log(keepPositive(arr))*/

/*function showName (myName) {
  console.log ('${myName}')
}
console.log ('Milana')*/

/*function showName (myName) {
  console.log ('${myName')
}
console.log ('John')*/

// function showDistance (time, speed) {
//   console.log (time * speed)
// }

// showDistance (5,4)


/*function showName (myName) {
  console.log (myName)
}

showName ('milana')*/

// function sum (x, y) {
//   console.log (x * y)
// }

// sum (2, 3)

// function showName (myName) {
//   console.log (myName)
// }

// showName ('Milana')

// function getRandom () {
//   return Math.floor(Math.random () * 5) + 1;
// }

// getRandom ()

// console.log ('veikia')

//  function showNameLength (myName, mySurname) {
//    return myName.length + mySurname.length;
//  }

//  console.log ("Vardenis", "Pavardenis")



 function showNameLength (myName, mySurname) {
   return myName.length + mySurname.length;
 }

 console.log ('Milana'.length)